<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingEnd="16dp"
                android:paddingStart="16dp"
                android:paddingTop="16dp">

    <TextView
        android:id="@+id/tv_title_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="标题："
        android:textSize="18sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/tv_title_title"
        android:layout_toEndOf="@id/tv_title_title"
        android:ellipsize="end"
        android:textSize="18sp"
        android:maxLines="2"
        android:layout_marginEnd="6dp"
        android:layout_toStartOf="@id/tv_complete_state"
        android:textStyle="bold"
        tools:text="去图书馆看书"/>

    <TextView
        android:id="@+id/tv_destination_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@id/tv_title_title"
        android:layout_below="@id/tv_title"
        android:layout_marginTop="6dp"
        android:text="目的地："/>

    <TextView
        android:id="@+id/tv_destination"
        android:layout_width="wrap_content"
        android:layout_toEndOf="@id/tv_destination_title"
        android:layout_height="wrap_content"
        android:layout_marginEnd="6dp"
        android:layout_alignTop="@id/tv_destination_title"
        android:layout_toStartOf="@id/tv_complete_state"
        android:ellipsize="end"
        tools:text="广州图书馆"/>

    <TextView
        android:id="@+id/tv_date_title"
        android:layout_width="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_height="wrap_content"
        android:layout_alignStart="@id/tv_title_title"
        android:layout_below="@id/tv_destination_title"
        android:text="建立日期："/>

    <TextView
        android:id="@+id/tv_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/tv_date_title"
        android:layout_alignTop="@id/tv_date_title"
        tools:text="2019.04.05"/>

    <TextView
        android:id="@+id/tv_complete_state"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        tools:text="待完成"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/tv_date_title"
        android:layout_marginTop="6dp"
        android:background="@color/grey"/>

</RelativeLayout>